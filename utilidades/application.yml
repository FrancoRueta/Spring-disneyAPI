spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/disneyAPI
    username: USUARIO
    password: CONTRASEÑA
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
server:
  port: 8080

